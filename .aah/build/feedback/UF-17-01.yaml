feedback_id: UF-17-01
wave: 17
source: user
started_at: '2026-09-05T14:05:00Z'
feedback_details: |
  Add a "Start Now" button to the Commissioner Draft Control UI (F-MOD-011, web/src/screens/commissioner/DraftControl.tsx) that lets the commissioner start the draft immediately, independent of whether/when it was scheduled. The server already exposes POST /drafts/:draftId/start (server/src/auction/routes.ts) which transitions a CREATED draft straight to RUNNING with no schedule gate — the gap is purely on the frontend: no UI anywhere currently calls this endpoint. Add a "Start Now" control to the Draft Controls panel, visible/enabled only when health.status === 'CREATED', that calls this endpoint and reports success/failure like the existing pause/resume controls. Context: user wants this to run a manual mock draft in the browser — commissioner sets up the draft, sets all owners to AUTO_AGENT, then needs to kick off the live auction on demand rather than waiting for a scheduled time.
steps:
- phase: plan
  input: >
    Add a Start Now control to the Draft Control panel (F-MOD-011, built and
    promoted, wave 14). Reuses the existing POST /drafts/:draftId/start
    endpoint (no backend change needed) — visible/enabled only when
    health.status === 'CREATED'.
  affected_modules:
  - commissioner-draft-control-ui
  blast_radius_features: []
  output: >
    Case A (implemented feature) rework. Edited F-MOD-011.md in place: added
    Start Now to the Description's item 1 and two new behavioral expectations
    (start transitions CREATED->RUNNING via existing POST /drafts/:id/start
    with no schedule gate; control hidden/disabled and a no-op when draft is
    not CREATED). Created superseding entry F-MOD-011-rework-01.md via
    core.plan.create_rework_entry, placed in wave 17 (current/last wave).
    feature-list.json and dag.json regenerated. Committed as 687734c.
  affected_features:
  - F-MOD-011-rework-01
  completed_at: "2026-09-05T14:15:00Z"
- phase: build
  input: "Build F-MOD-011-rework-01 via aah-feature-implementer, then boot for hands-on review."
  output: >
    F-MOD-011-rework-01 implemented and committed (28f0054): Start Now button
    added to DraftControl.tsx, gated on health.status === 'CREATED', calling
    existing POST /drafts/:id/start. New test suite (4 tests) + existing
    server suite (19 tests) pass, eslint clean. Verified live in browser:
    button appeared on CREATED draft and successfully started it (status
    changed to RUNNING). rework marked completed via
    core.build.rework complete.
  completed_at: "2026-09-05T14:35:00Z"
status: completed
